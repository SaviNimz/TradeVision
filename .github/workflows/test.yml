name: Run Tests on PR

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]  # Use Node.js 20.x version to match your environment

    steps:
      # Step 1: Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Set up Node.js environment
      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

      # Step 3: Cache Yarn dependencies
      - name: Cache Yarn dependencies
        uses: actions/cache@v3
        with:
          path: frontend/node_modules  # Specify path to frontend node_modules
          key: ${{ runner.os }}-yarn-${{ hashFiles('frontend/yarn.lock') }} # Hash based on frontend yarn.lock
          restore-keys: |
            ${{ runner.os }}-yarn-

      # Step 4: Install and Test Frontend
      - name: Install and Test Frontend
        working-directory: ./frontend   # Points to the frontend directory where the yarn.lock file exists
        run: |
          yarn install   # Install dependencies using the existing yarn.lock
          yarn test --ci # Run tests in CI mode
